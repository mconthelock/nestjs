CREATE TABLE "WEBFORM"."CR_DEVICEMST"
   (	"DNO" NUMBER(3,0) NOT NULL ENABLE,
	"DEVICE" VARCHAR2(200 BYTE) NOT NULL ENABLE,
	"STATUS" CHAR(1 BYTE) NOT NULL ENABLE,
	"DEPRECIATION" CHAR(1 BYTE),
	"TYPE_BRINGOUT" CHAR(1 BYTE),
	 PRIMARY KEY ("DNO")
    )
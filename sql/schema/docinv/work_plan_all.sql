CREATE TABLE WORK_PLAN_ALL (
    NFRMNO VARCHAR(255) NOT NULL,
    VORGNO VARCHAR(255) NOT NULL,
    CYEAR VARCHAR(255) NOT NULL,
    CYEAR2 INT NOT NULL,
    NRUNNO INT NOT NULL,
    WORKTYPE INT,
    WORKCATEGORY VARCHAR(255),
    PLANYEAR INT,
    REQNO VARCHAR(255),
    TITLE VARCHAR(255),
    DETAIL TEXT,
    VSYSNAME VARCHAR(255),
    DPDATE DATE,
    DPEDATE DATE,
    NTYPEID VARCHAR(255),
    VTYPEDESC VARCHAR(255),
    STATUS VARCHAR(255),
    REQUESTER VARCHAR(255),
    DEVELOPER VARCHAR(255),
    PRIMARY KEY (NFRMNO, VORGNO, CYEAR, CYEAR2, NRUNNO)
);
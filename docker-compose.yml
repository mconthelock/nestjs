version: '3.9'

services:
  api:
    container_name: nest-api

    build: .

    restart: unless-stopped

    ports:
      - "3010:3010"

    env_file:
      - .env
    
    extra_hosts:
      - "pdmapp4:172.21.255.5"